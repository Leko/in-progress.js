/* eslint-env mocha */
const { FormValueDetector } = require('../dist/inprogress')

describe(FormValueDetector.name, () => {
  describe('#constructor', () => {

  })
  describe('#getValues', () => {

  })
})
